import React from 'react';
import ReactDom from 'react-dom';
import 'antd/dist/antd.css';
import 

function Divider(text, orientation) {
	return (
		<Divider orientation="top">{text}</Divider>
	);
}

export default Divider;
